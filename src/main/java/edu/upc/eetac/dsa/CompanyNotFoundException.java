package edu.upc.eetac.dsa;

//This class is going to implement the Exception. As we can see this is class is empty
//We only need it to call in other classes like MyManager
//We have to add the extends and then the Exception(java lang class). The Exception is an extends of its class
public class CompanyNotFoundException extends Exception {
}
